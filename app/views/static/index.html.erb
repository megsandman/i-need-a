<div class="intro-header">
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <div class="intro-message">
                    <h1>I need a</h1><br>
                    <div class="dropdown">
                      <select class="dropdown-button activities">
                        <option value="0" >Venues</option>
                        <%= @activities.each do |activity| %>
                          <option value="<%= activity.id%>"><%= activity.name %></option>
                        <% end %>
                      </select>
                    </div>
                    <br><h1>in</h1><br>
                    <div class="dropdown">
                      <select class="dropdown-button neighborhoods">
                        <option value="0" >Neighborhood</option>
                        <%= @neighborhoods.each do |neighborhood| %>
                          <option value="<%= neighborhood.id%>"><%= neighborhood.name %></option>
                        <% end %>
                      </select>
                    </div>
                    <!-- <hr class="intro-divider"> -->
                    <br><div class="btn btn-lg btn-default" id="submit-button">HELP!</div>
                <br><div id="match-div">
                </div>
                </div>
            </div>
        </div>
    </div>
    <!-- /.container -->
</div>
<!-- /.intro-header -->